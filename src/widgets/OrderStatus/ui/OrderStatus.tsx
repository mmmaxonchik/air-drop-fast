import { useContext } from "react"
import { Loading } from "../../../shared/Loading"

function OrderStatus() {
  return (
    <div>
      {/* {apiOrderState === ApiOrderStates.isLoading ? <Loading /> : null}
      {apiOrderState === ApiOrderStates.Success ? (
        <h1>{order.OrderNumber}</h1>
      ) : null}
      {apiOrderState === ApiOrderStates.isLoading ? <h1>"Ошибка"</h1> : null} */}
    </div>
  )
}

export default OrderStatus
